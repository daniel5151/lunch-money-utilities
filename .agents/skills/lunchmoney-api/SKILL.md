---
name: lunchmoney-api
description: OpenAPI schema of the lunchmoney API
---

See `resources/api-1.json` for the lunchmoney openapi spec.

We use the v2 spec. NEVER USE THE v1 SPEC!

Do NOT use any built-in search tools / raw `grep` to explore the spec! Instead - shell out to `jq` to efficiently understand and explore the spec.
